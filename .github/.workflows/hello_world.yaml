name: Hello world workflow



on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3 #ai er god til at give denne kode, uden behov.
      - name: Run hello world
        run: echo "Hello world"
        shell: bash 